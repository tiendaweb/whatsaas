'use client';

/** Placeholder: lo reemplaza el equipo de Clientes/Empresas. */
export function ClientesView({ onOpen }: { onOpen?: (chatId: number) => void }) {
  void onOpen;
  return <div className="rounded-xl border border-dashed border-border p-6 text-sm text-muted-foreground">Clientes — en construcción</div>;
}
